namespace BowlingInterview;

public class Game
{
    //Feel free to add helper, properties, fields.... Whatever you need. 

    public void Roll(int pins)
    {
        //TODO: You roll the bowl down the alley. Two rolls per frame, except if strike
    }

    public int Score()
    {
        //TODO: Only Required to call this at end of game, but can build it to work at any point
        return 0;
    }
}